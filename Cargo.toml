[package]
name = "librtbit-clone-to-owned"
version = "0.1.1"
edition = "2024"
description = "Util traits to represent something that can be made owned and change type at the same time"
license = "MIT"
repository = "https://github.com/AusAgentSmith-org/librtbit-clone-to-owned"

[dependencies]
bytes = "1"

[lints.clippy]
all = { level = "warn", priority = -1 }

[lints.rust]
unused = "warn"
